We looked at the model architectures and solved a little part of a problem where 
we determine the weights and the bias of the gradient descent for the model to adjust the loss.


Use ollama and langchain for local.